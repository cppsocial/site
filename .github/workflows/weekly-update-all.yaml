name: Update Metadata Weekly

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: ./.github/workflows/update-metadata.yaml
    with:
      updater: update-all
      output-path: data/
