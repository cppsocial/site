import { initBooks } from "../directory/books";
import { initChannels } from "../directory/channels";
import { initDirectory } from "../directory/search";

initDirectory();
initBooks();
initChannels();
